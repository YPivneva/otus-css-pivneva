/** @type {import('tailwindcss').Config} */
module.exports = {
    theme: {
        fontSize: {
            bigest: '86px',
        },
        lineHeight: {
            bigestLine: '120%',
        }
    }
}
